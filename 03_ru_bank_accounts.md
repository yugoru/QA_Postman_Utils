# 03 — Банковские реквизиты РФ (БИК / расчётный счёт / корсчёт)

Все скрипты ниже вставляются в **Pre-request Script** и пишут результат в **Globals**.

**Как установить входные переменные**:
- **Рекомендуется**: прямо в скрипте через `pm.globals.set()` перед основным скриптом
- **Альтернатива**: вручную через интерфейс Postman (меню Globals)

---

## Переменные (Globals)

### Входные (необязательно)
- `bank_bik_in` — БИК для валидации или генерации счёта
- `bank_account_in` — расчётный счёт для валидации (20 цифр)
- `bank_corr_in` — корреспондентский счёт для валидации (20 цифр)

### Выходные
- `bank_bik` — сгенерированный БИК (9 цифр)
- `bank_bik_ok` — результат валидации БИК (true/false)
- `bank_bik_reason` — причина валидации БИК
- `bank_account` — сгенерированный расчётный счёт (20 цифр, без гарантии checksum)
- `bank_account_ok` — результат валидации расчётного счёта (true/false)
- `bank_account_reason` — причина валидации расчётного счёта
- `bank_corr` — сгенерированный корсчёт (20 цифр, без гарантии checksum)
- `bank_corr_ok` — результат валидации корсчёта (true/false)
- `bank_corr_reason` — причина валидации корсчёта

---

## 1) Генерация БИК (форматная)

**Что делает**: Генерирует БИК (банковский идентификационный код) в правильном формате.

**Формат**: 9 цифр (04XXYYYYZ, где 04 — код России, XX — код региона, YYYY — номер банка, Z — контрольная цифра)

**Входные переменные**: не требуются

**Выходная переменная**: `bank_bik` (9 цифр)

**Примечание**: Генерация форматная, контрольная цифра не проверяется.

```javascript
function padLeft(s, len, ch) {
  s = String(s);
  return s.length >= len ? s : (String(ch || "0").repeat(len - s.length) + s);
}

function randInt(min, max) {
  return Math.floor(Math.random() * (max - min + 1)) + min;
}

// БИК: 04XXYYYYZ
const countryCode = "04";
const regionCode = padLeft(randInt(1, 99), 2);
const bankNumber = padLeft(randInt(1, 9999), 4);
const controlDigit = String(randInt(0, 9));

const bik = countryCode + regionCode + bankNumber + controlDigit;

pm.globals.set("bank_bik", bik);
console.log("bank_bik =", bik);
```

---

## 2) Валидация БИК

**Что делает**: Проверяет формат БИК (9 цифр, начинается с 04).

**Входные переменные**:
- `bank_bik_in` — БИК для проверки. Если не задан, используется `bank_bik`

**Выходные переменные**:
- `bank_bik_ok` — результат валидации (true/false)
- `bank_bik_reason` — причина валидации

```javascript
function digitsOnly(v) {
  return String(v ?? "").replace(/\D+/g, "");
}

function validateBik(bik) {
  const s = digitsOnly(bik);
  
  if (s.length !== 9) {
    return { ok: false, reason: "БИК должен быть 9 цифр" };
  }
  
  if (!s.startsWith("04")) {
    return { ok: false, reason: "БИК должен начинаться с 04" };
  }
  
  return { ok: true, reason: "Валидный БИК" };
}

const bikToCheck = pm.globals.get("bank_bik_in") || pm.globals.get("bank_bik") || "";
const result = validateBik(bikToCheck);

pm.globals.set("bank_bik_ok", result.ok);
pm.globals.set("bank_bik_reason", result.reason);

console.log("bank_bik_ok =", result.ok);
console.log("bank_bik_reason =", result.reason);
```

---

## 3) Валидация расчётного счёта по БИК

**Что делает**: Проверяет валидность расчётного счёта (20 цифр) по алгоритму контрольной суммы с использованием БИК.

**Алгоритм**: Контрольная сумма рассчитывается как остаток от деления суммы произведений цифр на веса на 11.

**Входные переменные**:
- `bank_account_in` — расчётный счёт для проверки (20 цифр)
- `bank_bik_in` — БИК для проверки. Если не задан, используется `bank_bik`

**Выходные переменные**:
- `bank_account_ok` — результат валидации (true/false)
- `bank_account_reason` — причина валидации

```javascript
function digitsOnly(v) {
  return String(v ?? "").replace(/\D+/g, "");
}

function validateAccountByBik(account, bik) {
  const acc = digitsOnly(account);
  const b = digitsOnly(bik);
  
  if (acc.length !== 20) {
    return { ok: false, reason: "Расчётный счёт должен быть 20 цифр" };
  }
  
  if (b.length !== 9) {
    return { ok: false, reason: "БИК должен быть 9 цифр" };
  }
  
  // Веса для расчёта контрольной суммы
  const weights = [7, 1, 3, 7, 1, 3, 7, 1, 3, 7, 1, 3, 7, 1, 3, 7, 1, 3, 7, 1, 3, 7, 1];
  
  // Формируем строку: последние 3 цифры БИК + счёт
  const checkString = b.slice(-3) + acc;
  
  let sum = 0;
  for (let i = 0; i < checkString.length; i++) {
    sum += Number(checkString[i]) * weights[i];
  }
  
  const remainder = sum % 11;
  const isValid = remainder === 0;
  
  return {
    ok: isValid,
    reason: isValid ? "Валидный расчётный счёт" : `Невалидный расчётный счёт (остаток ${remainder})`
  };
}

const accountToCheck = pm.globals.get("bank_account_in") || "";
const bikToCheck = pm.globals.get("bank_bik_in") || pm.globals.get("bank_bik") || "";

const result = validateAccountByBik(accountToCheck, bikToCheck);

pm.globals.set("bank_account_ok", result.ok);
pm.globals.set("bank_account_reason", result.reason);

console.log("bank_account_ok =", result.ok);
console.log("bank_account_reason =", result.reason);
```

---

## 4) Валидация корсчёта по БИК

**Что делает**: Проверяет валидность корреспондентского счёта (20 цифр) по алгоритму контрольной суммы с использованием БИК.

**Алгоритм**: Аналогичен валидации расчётного счёта.

**Входные переменные**:
- `bank_corr_in` — корсчёт для проверки (20 цифр)
- `bank_bik_in` — БИК для проверки. Если не задан, используется `bank_bik`

**Выходные переменные**:
- `bank_corr_ok` — результат валидации (true/false)
- `bank_corr_reason` — причина валидации

```javascript
function digitsOnly(v) {
  return String(v ?? "").replace(/\D+/g, "");
}

function validateCorrByBik(corr, bik) {
  const c = digitsOnly(corr);
  const b = digitsOnly(bik);
  
  if (c.length !== 20) {
    return { ok: false, reason: "Корсчёт должен быть 20 цифр" };
  }
  
  if (b.length !== 9) {
    return { ok: false, reason: "БИК должен быть 9 цифр" };
  }
  
  // Веса для расчёта контрольной суммы
  const weights = [7, 1, 3, 7, 1, 3, 7, 1, 3, 7, 1, 3, 7, 1, 3, 7, 1, 3, 7, 1, 3, 7, 1];
  
  // Формируем строку: последние 3 цифры БИК + корсчёт
  const checkString = b.slice(-3) + c;
  
  let sum = 0;
  for (let i = 0; i < checkString.length; i++) {
    sum += Number(checkString[i]) * weights[i];
  }
  
  const remainder = sum % 11;
  const isValid = remainder === 0;
  
  return {
    ok: isValid,
    reason: isValid ? "Валидный корсчёт" : `Невалидный корсчёт (остаток ${remainder})`
  };
}

const corrToCheck = pm.globals.get("bank_corr_in") || "";
const bikToCheck = pm.globals.get("bank_bik_in") || pm.globals.get("bank_bik") || "";

const result = validateCorrByBik(corrToCheck, bikToCheck);

pm.globals.set("bank_corr_ok", result.ok);
pm.globals.set("bank_corr_reason", result.reason);

console.log("bank_corr_ok =", result.ok);
console.log("bank_corr_reason =", result.reason);
```

---

## 5) Генерация "заглушечного" расчётного счёта

**Что делает**: Генерирует расчётный счёт в правильном формате (20 цифр) без гарантии корректной контрольной суммы.

**Входные переменные**: не требуются

**Выходная переменная**: `bank_account` (20 цифр)

**Примечание**: Счёт генерируется форматно, контрольная сумма не проверяется. Для валидного счёта используйте валидацию с БИК.

```javascript
function padLeft(s, len, ch) {
  s = String(s);
  return s.length >= len ? s : (String(ch || "0").repeat(len - s.length) + s);
}

function randInt(min, max) {
  return Math.floor(Math.random() * (max - min + 1)) + min;
}

// Расчётный счёт: 20 цифр
const account = padLeft(randInt(1, 99999999999999999999), 20);

pm.globals.set("bank_account", account);
console.log("bank_account =", account);
console.warn("⚠️ ВНИМАНИЕ: Счёт сгенерирован форматно, контрольная сумма не проверяется!");
```

---

## 6) Генерация "заглушечного" корсчёта

**Что делает**: Генерирует корреспондентский счёт в правильном формате (20 цифр) без гарантии корректной контрольной суммы.

**Входные переменные**: не требуются

**Выходная переменная**: `bank_corr` (20 цифр)

**Примечание**: Корсчёт генерируется форматно, контрольная сумма не проверяется. Для валидного корсчёта используйте валидацию с БИК.

```javascript
function padLeft(s, len, ch) {
  s = String(s);
  return s.length >= len ? s : (String(ch || "0").repeat(len - s.length) + s);
}

function randInt(min, max) {
  return Math.floor(Math.random() * (max - min + 1)) + min;
}

// Корсчёт: 20 цифр
const corr = padLeft(randInt(1, 99999999999999999999), 20);

pm.globals.set("bank_corr", corr);
console.log("bank_corr =", corr);
console.warn("⚠️ ВНИМАНИЕ: Корсчёт сгенерирован форматно, контрольная сумма не проверяется!");
```

---

## Быстрый пример

```javascript
// Вставьте скрипт "1) Генерация БИК"
// Вставьте скрипт "2) Валидация БИК"

// Результат: {{bank_bik}}, {{bank_bik_ok}}
```

---

## При поддержке

Этот проект создан при поддержке [школы Эрмита](https://ermita.one/) — онлайн IT-школы для тестировщиков.

📚 [Курсы для тестировщиков](https://ermita.one/courses/) | 💬 [Телеграм-канал](https://t.me/ermita_one) | 🎮 [Тренажеры для QA](http://qahacking.ru/)

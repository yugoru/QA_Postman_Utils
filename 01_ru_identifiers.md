# 01 — Российские идентификаторы (ИНН/КПП/ОГРН/ОГРНИП/СНИЛС/ОКВЭД2)

Все скрипты ниже вставляются в **Pre-request Script** и пишут результат в **Globals**.

**Как установить входные переменные**:
- **Рекомендуется**: прямо в скрипте через `pm.globals.set("ru_region2", "77")` перед основным скриптом
- **Альтернатива**: вручную через интерфейс Postman (меню Globals)

---

## Переменные (Globals)

### Входные (необязательно)

* `ru_region2` — код региона (2 цифры), например `77` (Москва), `78` (Санкт-Петербург), `50` (Московская область)
  - **Как использовать**: установите через `pm.globals.set("ru_region2", "77")` в скрипте или вручную в Globals
  - Если не задан — используется случайный код региона (01-99)
  - **Примеры кодов регионов**:
    - `77` — Москва
    - `78` — Санкт-Петербург
    - `50` — Московская область
    - `47` — Ленинградская область

### Выходные

* `ru_id_inn_ul` — ИНН юридического лица (10 цифр, с контрольной суммой)
* `ru_id_inn_fl` — ИНН физического лица (12 цифр, с контрольными суммами)
* `ru_id_kpp` — КПП (9 символов, формат `NNNNXXNNN`, без контрольной суммы)
* `ru_id_ogrn` — ОГРН (13 цифр, с контрольной суммой)
* `ru_id_ogrnip` — ОГРНИП (15 цифр, с контрольной суммой)
* `ru_id_snils_raw` — СНИЛС (11 цифр без форматирования, с контрольной суммой)
* `ru_id_snils_fmt` — СНИЛС в формате `XXX-XXX-XXX YY` (например, `123-456-789 01`)
* `ru_id_okved2` — ОКВЭД2 (валидный формат, например `62.01` / `62.01.1` / `62.01.11`)

---

## Примеры использования

### Генерация ИНН для конкретного региона

Добавьте в начало Pre-request Script (перед основным скриптом):
```javascript
// Установка кода региона (Москва)
pm.globals.set("ru_region2", "77");
```


## 1) Сгенерировать ИНН ЮЛ (10) и сохранить в Globals

**Что делает**: Генерирует валидный ИНН юридического лица (10 цифр) с правильной контрольной суммой.

**Формат**: 10 цифр (первые 2 — код региона, затем 7 цифр номера, последняя — контрольная сумма)

**Входные переменные**:
- `ru_region2` (опционально) — код региона (2 цифры). Если не задан — используется случайный.

**Выходная переменная**: `ru_id_inn_ul` (10 цифр)

**Пример результата**: `7701234567`

```javascript
/**
 * ИНН ЮЛ (10): 9 цифр + контрольная сумма.
 * Использует ru_region2 (Globals) если задан.
 */

function digitsOnly(v) { return String(v ?? "").replace(/\D+/g, ""); }
function padLeft(s, len, ch) { s = String(s); return s.length >= len ? s : (String(ch || "0").repeat(len - s.length) + s); }
function randInt(min, max) { return Math.floor(Math.random() * (max - min + 1)) + min; }

function innUlControl(inn10) {
  const s = digitsOnly(inn10).slice(0, 9);
  const w = [2, 4, 10, 3, 5, 9, 4, 6, 8];
  let sum = 0;
  for (let i = 0; i < 9; i++) sum += Number(s[i]) * w[i];
  return String((sum % 11) % 10);
}

const region2 = padLeft(digitsOnly(pm.globals.get("ru_region2") || randInt(1, 99)), 2);
const body = region2 + padLeft(randInt(0, 9999999), 7); // 9 цифр
const inn = body + innUlControl(body + "0");

pm.globals.set("ru_id_inn_ul", inn);
console.log("ru_id_inn_ul =", inn);
```

---

## 2) Сгенерировать ИНН ФЛ (12) и сохранить в Globals

**Что делает**: Генерирует валидный ИНН физического лица (12 цифр) с правильными контрольными суммами.

**Формат**: 12 цифр (первые 2 — код региона, затем 8 цифр номера, последние 2 — контрольные суммы)

**Входные переменные**:
- `ru_region2` (опционально) — код региона (2 цифры). Если не задан — используется случайный.

**Выходная переменная**: `ru_id_inn_fl` (12 цифр)

**Пример результата**: `770123456789`

```javascript
/**
 * ИНН ФЛ (12): 10 цифр + 2 контрольные суммы.
 * Использует ru_region2 (Globals) если задан.
 */

function digitsOnly(v) { return String(v ?? "").replace(/\D+/g, ""); }
function padLeft(s, len, ch) { s = String(s); return s.length >= len ? s : (String(ch || "0").repeat(len - s.length) + s); }
function randInt(min, max) { return Math.floor(Math.random() * (max - min + 1)) + min; }

function innFlControl1(inn12) {
  const s = digitsOnly(inn12).slice(0, 10);
  const w = [7, 2, 4, 10, 3, 5, 9, 4, 6, 8];
  let sum = 0;
  for (let i = 0; i < 10; i++) sum += Number(s[i]) * w[i];
  return String((sum % 11) % 10);
}

function innFlControl2(inn12) {
  const s = digitsOnly(inn12).slice(0, 11);
  const w = [3, 7, 2, 4, 10, 3, 5, 9, 4, 6, 8];
  let sum = 0;
  for (let i = 0; i < 11; i++) sum += Number(s[i]) * w[i];
  return String((sum % 11) % 10);
}

const region2 = padLeft(digitsOnly(pm.globals.get("ru_region2") || randInt(1, 99)), 2);
const body = region2 + padLeft(randInt(0, 99999999), 8); // 10 цифр
const c1 = innFlControl1(body + "00");
const c2 = innFlControl2(body + c1 + "0");
const inn = body + c1 + c2;

pm.globals.set("ru_id_inn_fl", inn);
console.log("ru_id_inn_fl =", inn);
```

---

## 3) Сгенерировать КПП (9) и сохранить в Globals

**Что делает**: Генерирует валидный КПП (код причины постановки на учёт) в правильном формате.

**Формат**: 9 символов `NNNNXXNNN`, где:
- `NNNN` — код налогового органа (первые 2 цифры — код региона)
- `XX` — причина постановки на учёт (2 цифры)
- `NNN` — порядковый номер постановки (3 цифры)

**Входные переменные**:
- `ru_region2` (опционально) — код региона (2 цифры). Если не задан — используется случайный.

**Выходная переменная**: `ru_id_kpp` (9 символов)

**Пример результата**: `770101001`

**Примечание**: КПП не имеет контрольной суммы, только форматная проверка.

```javascript
/**
 * КПП: NNNNXXNNN
 * Генерация форматная (контрольной суммы нет).
 * Использует ru_region2 (Globals) если задан.
 */

function digitsOnly(v) { return String(v ?? "").replace(/\D+/g, ""); }
function padLeft(s, len, ch) { s = String(s); return s.length >= len ? s : (String(ch || "0").repeat(len - s.length) + s); }
function randInt(min, max) { return Math.floor(Math.random() * (max - min + 1)) + min; }

const region2 = padLeft(digitsOnly(pm.globals.get("ru_region2") || randInt(1, 99)), 2);
const insp = padLeft(randInt(1, 999), 3);
const reason = padLeft(randInt(1, 99), 2);
const seq = padLeft(randInt(1, 999), 3);

const kpp = region2 + insp + reason + seq;

pm.globals.set("ru_id_kpp", kpp);
console.log("ru_id_kpp =", kpp);
```

---

## 4) Сгенерировать ОГРН (13) и сохранить в Globals

**Что делает**: Генерирует валидный ОГРН (основной государственный регистрационный номер) юридического лица с правильной контрольной суммой.

**Формат**: 13 цифр (12 цифр номера + 1 контрольная цифра)

**Входные переменные**: не требуются

**Выходная переменная**: `ru_id_ogrn` (13 цифр)

**Пример результата**: `1027700132195`

**Примечание**: Контрольная сумма вычисляется как остаток от деления на 11, затем на 10.

```javascript
/**
 * ОГРН (13): 12 цифр + контрольная (mod 11, затем mod 10).
 */

function digitsOnly(v) { return String(v ?? "").replace(/\D+/g, ""); }
function padLeft(s, len, ch) { s = String(s); return s.length >= len ? s : (String(ch || "0").repeat(len - s.length) + s); }
function randInt(min, max) { return Math.floor(Math.random() * (max - min + 1)) + min; }

function ogrnCtrl(ogrn12) {
  const n = BigInt(digitsOnly(ogrn12).slice(0, 12));
  const mod = n % 11n;
  return String(Number(mod % 10n));
}

const body = padLeft(randInt(1, 999999999999), 12);
const ogrn = body + ogrnCtrl(body);

pm.globals.set("ru_id_ogrn", ogrn);
console.log("ru_id_ogrn =", ogrn);
```

---

## 5) Сгенерировать ОГРНИП (15) и сохранить в Globals

**Что делает**: Генерирует валидный ОГРНИП (основной государственный регистрационный номер индивидуального предпринимателя) с правильной контрольной суммой.

**Формат**: 15 цифр (14 цифр номера + 1 контрольная цифра)

**Входные переменные**: не требуются

**Выходная переменная**: `ru_id_ogrnip` (15 цифр)

**Пример результата**: `304500116000157`

**Примечание**: Контрольная сумма вычисляется как остаток от деления на 13, затем на 10.

```javascript
/**
 * ОГРНИП (15): 14 цифр + контрольная (mod 13, затем mod 10).
 */

function digitsOnly(v) { return String(v ?? "").replace(/\D+/g, ""); }
function padLeft(s, len, ch) { s = String(s); return s.length >= len ? s : (String(ch || "0").repeat(len - s.length) + s); }
function randInt(min, max) { return Math.floor(Math.random() * (max - min + 1)) + min; }

function ogrnipCtrl(ogrn14) {
  const n = BigInt(digitsOnly(ogrn14).slice(0, 14));
  const mod = n % 13n;
  return String(Number(mod % 10n));
}

const body = padLeft(randInt(1, 99999999999999), 14);
const ogrnip = body + ogrnipCtrl(body);

pm.globals.set("ru_id_ogrnip", ogrnip);
console.log("ru_id_ogrnip =", ogrnip);
```

---

## 6) Сгенерировать СНИЛС (11) и сохранить в Globals

**Что делает**: Генерирует валидный СНИЛС (страховой номер индивидуального лицевого счёта) с правильной контрольной суммой. Сохраняет два варианта: без форматирования и с форматированием.

**Формат**: 
- Без форматирования: 11 цифр (9 цифр номера + 2 контрольные цифры)
- С форматированием: `XXX-XXX-XXX YY` (например, `123-456-789 01`)

**Входные переменные**: не требуются

**Выходные переменные**: 
- `ru_id_snils_raw` — СНИЛС без форматирования (11 цифр)
- `ru_id_snils_fmt` — СНИЛС с форматированием (формат `XXX-XXX-XXX YY`)

**Пример результата**: 
- `ru_id_snils_raw` = `12345678901`
- `ru_id_snils_fmt` = `123-456-789 01`

**Примечание**: Контрольная сумма вычисляется по специальному алгоритму. Если сумма равна 100 или 101, контрольная сумма становится "00".

```javascript
/**
 * СНИЛС: 9 цифр + контрольная сумма (2 цифры).
 * Дополнительно сохраняем форматированную строку XXX-XXX-XXX YY.
 */

function digitsOnly(v) { return String(v ?? "").replace(/\D+/g, ""); }
function padLeft(s, len, ch) { s = String(s); return s.length >= len ? s : (String(ch || "0").repeat(len - s.length) + s); }
function randInt(min, max) { return Math.floor(Math.random() * (max - min + 1)) + min; }

function snilsChecksum(num9) {
  const s = digitsOnly(num9).slice(0, 9);
  let sum = 0;
  for (let i = 0; i < 9; i++) sum += Number(s[i]) * (9 - i);

  if (sum < 100) return padLeft(sum, 2);
  if (sum === 100 || sum === 101) return "00";

  const mod = sum % 101;
  return (mod === 100) ? "00" : padLeft(mod, 2);
}

function snilsFormat(v11) {
  const v = digitsOnly(v11);
  if (v.length !== 11) return String(v11 ?? "");
  return `${v.slice(0, 3)}-${v.slice(3, 6)}-${v.slice(6, 9)} ${v.slice(9, 11)}`;
}

const body = padLeft(randInt(1, 999999999), 9);
const raw = body + snilsChecksum(body);
const fmt = snilsFormat(raw);

pm.globals.set("ru_id_snils_raw", raw);
pm.globals.set("ru_id_snils_fmt", fmt);

console.log("ru_id_snils_raw =", raw);
console.log("ru_id_snils_fmt =", fmt);
```

---

## 7) Сгенерировать ОКВЭД2 (валидный формат) и сохранить в Globals

**Что делает**: Генерирует валидный код ОКВЭД2 (общероссийский классификатор видов экономической деятельности) в одном из трёх форматов.

**Формат**: Один из трёх вариантов (выбирается случайно):
- `XX.XX` — класс (2 цифры.2 цифры), например `62.01`
- `XX.XX.X` — подкласс (2 цифры.2 цифры.1 цифра), например `62.01.1`
- `XX.XX.XX` — группа (2 цифры.2 цифры.2 цифры), например `62.01.11`

**Входные переменные**: не требуются

**Выходная переменная**: `ru_id_okved2` (строка с точками)

**Пример результата**: `62.01` или `62.01.1` или `62.01.11`

**Примечание**: ОКВЭД2 не имеет контрольных цифр, только форматная проверка.

```javascript
/**
 * ОКВЭД2 не имеет контрольных цифр.
 * Генерация валидного формата:
 * - XX.XX (класс)
 * - XX.XX.X (подкласс)
 * - XX.XX.XX (группа)
 */

function padLeft(s, len, ch) { s = String(s); return s.length >= len ? s : (String(ch || "0").repeat(len - s.length) + s); }
function randInt(min, max) { return Math.floor(Math.random() * (max - min + 1)) + min; }

const a = padLeft(randInt(1, 99), 2);
const b = padLeft(randInt(0, 99), 2);
const variant = randInt(1, 3);

let okved2 = `${a}.${b}`;
if (variant === 2) okved2 = `${a}.${b}.${randInt(0, 9)}`;
if (variant === 3) okved2 = `${a}.${b}.${padLeft(randInt(0, 99), 2)}`;

pm.globals.set("ru_id_okved2", okved2);
console.log("ru_id_okved2 =", okved2);
```

---

## Быстрый пример

```javascript
// Установка параметров
pm.globals.set("ru_region2", "77");

// Вставьте скрипт "1) Сгенерировать ИНН ЮЛ"

// Результат: {{ru_id_inn_ul}}
```

---

## При поддержке

Этот проект создан при поддержке [школы Эрмита](https://ermita.one/) — онлайн IT-школы для тестировщиков.

📚 [Курсы для тестировщиков](https://ermita.one/courses/) | 💬 [Телеграм-канал](https://t.me/ermita_one) | 🎮 [Тренажеры для QA](http://qahacking.ru/)
